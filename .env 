# Local Development
PORT=3000
NODE_ENV=development

# MongoDB Local
MONGO_URI=mongodb://localhost:27017/yemen-chat

# JWT Secret
JWT_SECRET=dev_secret_key_local_only

# CORS
CORS_ORIGIN=*

# API URLs
API_URL=http://localhost:3000/api
